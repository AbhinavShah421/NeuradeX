fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# RabbitMQ (async)
aio-pika==9.4.1

# PostgreSQL / TimescaleDB
asyncpg==0.29.0

# MongoDB
motor==3.3.2

# Redis
redis==5.0.1

# HTTP client (Groww API)
httpx==0.27.0

# Yahoo Finance (fallback) — 1.x required: 0.2.x gets blocked by Yahoo (non-JSON
# "Expecting value" responses); 1.x impersonates a browser via curl_cffi
yfinance==1.5.1

# News
requests==2.32.3

# Timezone support
tzdata==2024.1

python-dotenv==1.0.0
python-json-logger==2.0.7
