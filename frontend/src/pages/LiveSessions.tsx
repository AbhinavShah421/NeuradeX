import React from 'react';
import SessionManager from '../components/SessionManager';

// Unified view of all session types (replay + paper).
const LiveSessions: React.FC = () => <SessionManager />;

export default LiveSessions;
